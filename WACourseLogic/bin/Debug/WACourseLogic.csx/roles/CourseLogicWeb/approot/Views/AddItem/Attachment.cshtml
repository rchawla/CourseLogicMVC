
<script type="text/javascript">
    function CuteWebUI_AjaxUploader_OnPostback() {
        //submit the form after the file have been uploaded:
        alert("submit call");
        //document.forms[0].submit();
    }
    function Temp_Click() {
        alert("");
        var uploadobj = document.getElementById('myuploader');
        if (uploadobj.getqueuecount() > 0) {
            uploadobj.startupload();
        }
        else {
            alert("Please browse files for upload");
        }
    }

    function CuteWebUI_AjaxUploader_OnStart() {
        alert("Star");
    }
</script>
<form enctype="multipart/form-data" >@MvcHtmlString.Create(@ViewData["uploaderhtml"].ToString())</form>
<input type="button" value="Submit" onclick="Temp_Click();" />
@if (ViewData["UploadedMessage"] != null)
{
@MvcHtmlString.Create(@ViewData["UploadedMessage"].ToString())
}
